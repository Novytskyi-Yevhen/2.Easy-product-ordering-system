@model ViewTask.Models.ShoppingOrder

<h3>Your order is ordered xD</h3>
<h6>Your products will be shipped at:</h6>

@if (Model.Address == "Kyiv" || Model.Address == "Kharkiv") {
    @:Shipping is free.
}
else { 
    @:Shipping will cost you $20.
}